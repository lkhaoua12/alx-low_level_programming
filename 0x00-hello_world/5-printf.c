# include <stdio.h>
/**
 * main entry point
 * dipllay output using printf
 * return 0 if succes
 */
int main(void)
{
        printf("with proper grammar, but the outcome is a piece of art,");
        return 0;
}
